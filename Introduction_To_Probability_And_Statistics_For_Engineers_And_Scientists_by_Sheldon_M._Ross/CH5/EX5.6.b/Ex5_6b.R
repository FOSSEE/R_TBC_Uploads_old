#When C is put to use, one other machine(either A or B ) will still be working . 
#The probability of this machine or C failing is equal due to 
#the memoryless propoerty of exponential random variables.

cat("The probability that machine which is still operable is machine C is ",1/2)