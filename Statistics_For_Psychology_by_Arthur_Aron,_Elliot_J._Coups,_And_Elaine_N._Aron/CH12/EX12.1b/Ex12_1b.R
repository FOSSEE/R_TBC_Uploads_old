                                      # Page no. : 497

# How are you doing?

# 4th Question

a <- -1.23   # Regression constant
b <- 6.11   # Regression coefficient

# (a)

x <- 2.00   # Predictor variable

y_cap <- a + (b * x)   # Predicted score (Linear predictor)

cat("Predicted score (Linear predictor) is", y_cap)

# (b)

x <- 4.87   # Predictor variable

y_cap <- a + (b * x)   # Predicted score (Linear predictor)

cat("Predicted score (Linear predictor) is", round(y_cap, 2))

# (a)

x <- -1.92   # Predictor variable

y_cap <- a + (b * x)   # Predicted score (Linear predictor)

cat("Predicted score (Linear predictor) is", round(y_cap, 2))